Branch created to keep code samples for https://github.com/hcoles/pitest/issues/582

In `cli-with-zip/` run `mvn compile` and then `./pitest.sh` to run a pitest analysis with a zip file in the
`--classPath` argument.
